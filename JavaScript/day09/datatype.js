//"hello".replace();
const a = "hello";
// a.includes()

const b = 123;

const c = true;

const d = ["커피", "빵", "아이스크림"];
// d.map(() => {});

const e = document.createElement("div");
// e.appendChild();

const f = { name: "kim", age: 21 };
// f.keys()

// type-casting
// 명시적 (explict) : Number, String, Boolean, Object, Array
// 암묵적(implict) : +, !!, [], {},
