// 변수 [const, let, var]

// 컴퓨터= 0 or 1 <-> 기계어 <-> 어셈블리어 <-> C,JAVA,PYTHON .. <-> REACT,VUE,SRPING-BOOT <->인간 : 언어

// 데이터 타입 : 기본, 참조

// 기본 : Number, String, bolean, Undefined[정의 돼지않음]

// 타입 변환(type - casting)
// Number, String, bolean, Undefined

const a = Number("1004");
const b = String(234);

Boolean(); //true or false ,  문자열 ->" ", 숫자 -> 0
